<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="some.project.com"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />
    <uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:label="@string/app_name"
            android:name=".LaunchpadProjectActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="AddEvent"
			android:label="@string/app_name">
		</activity>
		 <activity android:name=".ModernLayout"
			android:label="@string/app_name">
		</activity>
		<activity 
		    android:label="Successful Add"
		    android:name=".SuccessfulAddActivity" 
		/>
		<activity 
		    android:label="Unsuccessful Add"
		    android:name=".UnsuccessfulAddActivity" 
		/>
		<activity 
		    android:label="Search Event"
		    android:name=".SearchActivity" 
		/>
		<activity 
		    android:label="Search Results"
		    android:name=".SimpleEventListActivity" 
		/>
				<activity 
		    android:label="@string/app_name"
		    android:name=".AlternativeLayout" 
		/>
    </application>
    

</manifest>